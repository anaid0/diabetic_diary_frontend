class UseCase
  constructor: ->
    
  start: =>
    @getEntries()

  getEntries: =>

  setInitialEntries: (entries) =>

  #addNewEntry: (entry) =>
    #@entries.push(entry)

  #deleteEntry: =>
    #@entries.remove(entry)

  getEntryToEdit: (entryId) =>

  entryFound: (entry, iList, mList) =>

  getInsulinTypes: (userId) =>
    console.log("jestem w usecase.getInsulinTypes")

  getMeasurementTypes: (userId) =>
    console.log("jestem w usecase.getMeasureTypes")